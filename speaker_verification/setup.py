from setuptools import setup, find_packages

setup(
    name="speaker_verification",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here, e.g.:
        # "numpy",
        # "torch",
    ],
    author="Your Name",
    description="Speaker verification and registration package",
)